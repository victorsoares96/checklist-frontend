import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  inputFile: {
    display: 'flex',
    flexDirection: 'column',
    content: 'Insira um anexo',
  }
}));

export default useStyles;