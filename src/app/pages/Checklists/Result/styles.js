import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles((theme) => ({
  title: {
    fontWeight: 600
  },
  subTitle: {
    fontWeight: 600
  },
  description: {
    marginTop: 30,
    fontWeight: 600,
    fontSize: 16
  },
  buttonGoBack: {
    color: '#fff',
    fontWeight: 600
  }
}));